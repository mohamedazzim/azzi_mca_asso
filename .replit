language = "nodejs"
run = "npm run dev"
entrypoint = "npm run dev"

[env]
# Add your MongoDB connection string here (update in Replit secrets)
MONGODB_URI = "mongodb+srv://mohamed_azzim:Azzim%4003@clusterlearning.pjv5awa.mongodb.net/college_management?retryWrites=true&w=majority&appName=ClusterLearning"

[nix]
# Use Node.js 18 for compatibility
pkgs = ["nodejs-18_x"]

# Optionally include more system dependencies if needed
# pkgs = ["nodejs-18_x", "imagemagick", "tesseract"]
